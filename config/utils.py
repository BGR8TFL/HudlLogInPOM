## For future use:



# from selenium.webdriver.support.ui import WebDriverWait
# from selenium.webdriver.remote.webdriver import WebDriver
#
# def wait_for(driver: WebDriver, expected_condition, timeout=10) -> bool:
#     """
#     Wait for an expected condition to be met within a specified timeout.
#     Returns True if the condition is met, False if the timeout is reached.
#     """
#     wait = WebDriverWait(driver, timeout)
#     try:
#         wait.until(expected_condition)
#         return True
#     except:
#         return False